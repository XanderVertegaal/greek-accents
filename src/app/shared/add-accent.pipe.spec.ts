import { AddAccentPipe } from './add-accent.pipe';

describe('AddAccentPipe', () => {
  it('create an instance', () => {
    const pipe = new AddAccentPipe();
    expect(pipe).toBeTruthy();
  });
});
